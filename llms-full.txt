# Spark Agent Full Context

Spark is a local-first personal agent.

The user talks to Spark through Telegram.

Spark uses the user's chosen LLM provider to answer.

Spark can remember useful facts, run missions, and help with research or building.

## Main URLs

- Human docs: https://agent.sparkswarm.ai/docs
- Install page: https://agent.sparkswarm.ai/install
- Human install safety: https://agent.sparkswarm.ai/docs/install-safety/
- Human commands: https://agent.sparkswarm.ai/docs/commands/
- Human providers: https://agent.sparkswarm.ai/docs/providers/
- Human Railway/VPS: https://agent.sparkswarm.ai/docs/railway-vps/
- Human suites: https://agent.sparkswarm.ai/docs/suites/
- Human feedback: https://agent.sparkswarm.ai/docs/feedback/
- Human self-improvement: https://agent.sparkswarm.ai/docs/self-improvement/
- Human lifecycle: https://agent.sparkswarm.ai/docs/lifecycle/
- Human troubleshooting: https://agent.sparkswarm.ai/docs/troubleshooting/
- Human security: https://agent.sparkswarm.ai/docs/security/
- Install safety: https://agent.sparkswarm.ai/docs/install-safety.md
- Spark commands: https://agent.sparkswarm.ai/docs/commands.md
- Spark providers: https://agent.sparkswarm.ai/docs/providers.md
- Spark Railway/VPS: https://agent.sparkswarm.ai/docs/railway-vps.md
- Spark suites: https://agent.sparkswarm.ai/docs/suites.md
- Spark feedback: https://agent.sparkswarm.ai/docs/feedback.md
- Self-improvement: https://agent.sparkswarm.ai/docs/self-improvement.md
- Lifecycle: https://agent.sparkswarm.ai/docs/lifecycle.md
- Troubleshooting: https://agent.sparkswarm.ai/docs/troubleshooting.md
- Security: https://agent.sparkswarm.ai/docs/security.md
- Agent instructions: https://agent.sparkswarm.ai/docs/AGENTS.md
- Install commands: https://agent.sparkswarm.ai/install/commands.json

## Default Starter Kit

The default bundle is `telegram-starter`.

It includes:

- `spark-telegram-bot`
- `spark-intelligence-builder`
- `domain-chip-memory`
- `spawner-ui`
- `spark-researcher`
- `spark-character`

## Plain-Language Module Map

Spark Telegram Bot is the front door.

The user sends Spark messages in Telegram.

Spark Intelligence Builder is the runtime router.

It connects Spark to the chosen LLM provider.

Domain Chip Memory is the starter memory layer.

It lets Spark save and recall useful facts.

Spawner UI is Mission Control.

It starts and tracks bigger work.

Spark Researcher gathers context and evidence.

Spark Researcher also runs bounded improvement loops.

Spark Character keeps identity, role, voice, and permission behavior stable.

Domain chips teach Spark how to score and improve one domain.

Feedback and PRs should be routed through the repo map at `/docs/feedback/`.

Benchmarks keep those loops honest.

Autostart brings Spark back after login.

## Domain Chips And Loops

A domain chip is both a reusable tool Spark can call and a bounded specialization loop Spark Researcher can run.

OTOLO/autoloops are bounded Researcher loops: one task, one score, a few rounds, then review.

The chip owns domain behavior:

- `evaluate`
- `suggest`
- `packets`
- `watchtower`

Spark Researcher owns loop execution, ledger, queue, memory index, vault generation, and review policy.

The simple loop is:

```text
workflow -> Researcher -> benchmark -> memory packet -> better next run
```

Examples:

- `spark-domain-chip-labs`: meta-chip for chip quality, methodology, benchmarks, and recursive improvement.
- `spark-character`: public identity, voice consistency, permission posture, and character evolution runtime.
- `spark-personality-chip-labs`: public modular personality chip and profile experiments.
- `spark-voice-comms`: public voice communication hooks and voice profile examples.
- `domain-chip-startup-yc`: startup advice and founder-decision doctrine.
- `domain-chip-crypto-trading`: trading doctrine, backtesting, and paper-trading style validation.
- `startup-bench`: benchmark for operating a simulated startup over time.

Spark Swarm network submission and hosted workspace review are upcoming private surfaces. Do not tell public users they must install Spark Swarm to experiment locally.

Safe prompt shape:

```text
Use Spark Researcher's domain-chip authoring docs to design a new Spark domain chip for <domain>.
Treat it as both a reusable tool my Spark agent can call and a bounded specialization loop Spark Researcher can run and score.
Inspect ~/.spark/modules/spark-researcher/source/docs/master_chip_v2 before writing files.
Build the smallest v0 with evaluate, suggest, packets, watchtower, and tests.
Ask before risky changes.
```

## Safe Install Flow

Never pipe remote installer scripts into a shell.

Download the installer first.

Run dry-run first.

Explain the plan.

Ask before the real install.

Do not send Telegram tokens or LLM keys to websites.

## Current Installer Pins

- Spark CLI release ref: `spark-cli-public-installer-2026-06-03-r24-v2`
- Spark CLI commit: `fc49c16a97ac5b69aaf27daea55918a40a28ad0c`
- Node: `22.18.0`
- Python: `3.11`
- uv: `0.11.7`

## Allowed Installer Network Destinations

- `agent.sparkswarm.ai`
- `github.com/vibeforge1111/spark-cli`
- `github.com/astral-sh/uv`
- `nodejs.org`

Stop if the plan requires another destination.

## Daily Checks

```bash
spark guide
spark live start
spark live status
spark providers status
spark providers test --role chat
spark verify --onboarding
```

## Provider Setup

Default to one provider for Agent and Mission, or split them during setup.

Useful commands:

```bash
spark recommend llms
spark setup
spark providers list
spark providers status
spark providers test --role chat
```

## Hosted Railway And VPS

Use hosted Spark only when the user wants always-on Telegram, Mission Control,
and persistent workspaces. Keep the local installer path as the default first
experience.

Recommended hosted shape:

- Spark Live / Spawner service for Mission Control, provider execution, previews,
  board state, and workspaces.
- Spark Telegram Bot service for long polling, access control, and private
  mission relay.
- Private service-to-service URL for `SPAWNER_UI_URL`.
- Public protected Spawner URL for `SPAWNER_UI_PUBLIC_URL`.
- Shared `SPARK_BRIDGE_API_KEY` between bot and Spawner.
- Separate `SPARK_UI_API_KEY` for browser login.

Hosted provider rule: use API keys or reachable private model endpoints. Do not
copy personal Codex OAuth, Claude Code OAuth, browser profiles, `~/.codex`, or
desktop auth into Railway or a VPS.

Railway deploy note: use `railway up` for code changes and `railway redeploy`
for same-code restarts or environment changes. Healthchecks should return HTTP
200 and the app should listen on Railway's `PORT`.

## Repair

If Telegram is quiet:

```bash
spark fix telegram
spark logs spark-telegram-bot --lines 80
```

If replies look like memory labels:

```bash
spark providers status
spark verify --deep
```

If Spawner is unreachable:

```bash
spark fix spawner
spark logs spawner-ui --lines 80
```

## Security

```bash
spark security audit
spark verify --provenance
spark verify --installers --hosted-installers
spark support bundle
```

Review support reports before sharing.

## Verification Receipts

- Checksums: https://agent.sparkswarm.ai/install/checksums.txt
- Manifest: https://agent.sparkswarm.ai/install/release-manifest.json
- Attestations: https://agent.sparkswarm.ai/install/attestations.md
- Signatures: https://agent.sparkswarm.ai/install/signatures.md
